import zipfile
import os
with zipfile.ZipFile('archive.zip','r') as zip_ref:
  zip_ref.extractall('/content/data')

  print(os.listdir('/content/data')

  so using this only we can open or collect the csv file 
  then only future we write program 
  After getting dataset only we can produce futher 
  steps
  •write program 
  •output 
